apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketsio.fullname" . }}
  labels:
    {{- include "ticketsio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticketsio.selectorLabels" . | nindent 4 }}
