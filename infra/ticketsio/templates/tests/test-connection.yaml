apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ticketsio.fullname" . }}-test-connection"
  labels:
    {{- include "ticketsio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ticketsio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
